var React = require('react');

var Timer = React.createClass({

    render() {
        return(
            <div>
                timer down
            </div>
        )
    }
})

module.exports = Timer;